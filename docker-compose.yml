services:
  mongo:
    image: mongo
    ports:
      - '27017:27017'
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: sapan
      POSTGRES_PASSWORD: ordbms
    ports:
      - '5432:5432'
